import { HighestVoteDirective } from './highest-vote.directive';

describe('HighestVoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestVoteDirective();
    expect(directive).toBeTruthy();
  });
});
